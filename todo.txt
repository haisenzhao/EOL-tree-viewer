fix the tooltip location so at least the top is on screen.  find a way to show detail with a stationary element.  Consider using http://swip.codylindley.com/DOMWindowDemo.html#example7

replace the xml api calls with html api calls.  (reduces the number of round-trips to the server by 2/3).

try to make sibling sort order at least stable

show all ancestors instead of just the next one up

center the images

consider giving the path to the current page's subtree more area, or at least giving it a different color